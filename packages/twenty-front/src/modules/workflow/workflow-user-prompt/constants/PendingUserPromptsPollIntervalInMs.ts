// A workflow run is enqueued, so the prompt appears a moment after the record
// change. This is the delay between that change and the modal showing up.
export const PENDING_USER_PROMPTS_POLL_INTERVAL_IN_MS = 4_000;
