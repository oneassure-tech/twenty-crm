// Sentinel used as the selected option id when the user picks the free-text
// "Other" choice instead of one of the options configured on the step.
export const USER_PROMPT_OTHER_OPTION_ID = '__other__';
